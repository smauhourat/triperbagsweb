const onClickBody = document.querySelector("body");

function showScreenSize() {
    console.log('Window Size: ' + window.innerWidth + ' - ' + window.innerHeight);
}

toggle.addEventListener("click", showScreenSize, false);